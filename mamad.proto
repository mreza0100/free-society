syntax = "proto3";

option go_package = "/generated";

service MamadService {
      rpc Hi (HiRequest) returns (HiResponse);
}


message HiRequest {
      string name = 1;
}

message HiResponse {
      repeated int64 response = 1;
      bool ok = 2;
}

